# USB HID (wired) — enabled by default on nice_nano_v2
CONFIG_ZMK_USB=y

# BLE (wireless) — enabled by default on nice_nano_v2
CONFIG_ZMK_BLE=y

# Handy for debugging over USB CDC ACM; enable temporarily if needed.
# CONFIG_ZMK_USB_LOGGING=y
